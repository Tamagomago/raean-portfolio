export type Experience = {
  title: string;
  at: string;
  key_points: string[];
  date_duration: string;
  image: string;
};

export type Tool = {
  name: string;
  logo: string;
};

export type Project = {
  title: string;
  description: string;
  tech_stack: string[];
  link: string;
  image: string;
};
